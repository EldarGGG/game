# 👨‍💻 Руководство разработчика - Eco Ranger

## 📂 Структура проекта

```
GAME/
├── main.py                 # Главный файл игры (все классы и логика)
├── requirements.txt        # Зависимости Python
├── README.md              # Основная документация
├── ИНСТРУКЦИЯ.md          # Инструкция на русском и казахском
├── DEVELOPER_GUIDE.md     # Руководство разработчика
└── start_game.bat         # Скрипт запуска для Windows
```

## 🏗️ Архитектура игры

### Основные компоненты:

#### 1. **Game** (главный класс)
Управляет всем игровым процессом:
- Состояния игры (меню, игра, пауза, game over)
- Загрузка уровней
- Обработка событий
- Отрисовка

```python
Методы:
- new_game()           # Начать новую игру
- load_level(level)    # Загрузить уровень
- handle_events()      # Обработка ввода
- update()             # Обновление логики
- draw()               # Отрисовка
```

#### 2. **Player** (игрок)
Класс персонажа Eco Ranger:
- Физика движения (гравитация, прыжки)
- Анимация ходьбы и поворотов
- Сбор мусора
- Отрисовка персонажа

```python
Свойства:
- vel_x, vel_y        # Скорость
- carrying_trash      # Количество мусора
- on_ground          # На земле?
- facing_right       # Направление взгляда

Методы:
- update()           # Обновление
- jump()             # Прыжок
- collect_trash()    # Собрать мусор
- draw_character()   # Отрисовка
```

#### 3. **Trash** (мусор)
Различные типы мусора:
- plastic (пластик)
- paper (бумага)
- glass (стекло)
- metal (металл)

```python
Свойства:
- trash_type         # Тип мусора
- moving            # Движется ли (ветер)
- needs_drone       # Требует дрона
```

#### 4. **Obstacle** (препятствия)
Препятствия на уровне:
- tree (дерево)
- car (машина)
- toxic (токсичная бочка)

```python
Свойства:
- obs_type          # Тип препятствия
- moving            # Движется ли
- toxic             # Токсичное?
```

#### 5. **RecyclingStation** (станция переработки)
Пункт сдачи мусора:
- Анимированный символ переработки
- Проверка коллизий с игроком

#### 6. **Drone** (дрон-помощник)
Помощник на 3 уровне:
- Автоматический сбор специального мусора
- Анимация пропеллеров
- Следование за игроком

```python
Методы:
- toggle()          # Включить/выключить
- collect_trash()   # Собрать мусор
- draw_drone()      # Отрисовка
```

#### 7. **Particle** (частицы)
Визуальные эффекты:
- Эффекты сбора мусора
- Эффекты урона
- Эффекты успеха

## 🎮 Игровые состояния (GameState)

```python
class GameState(Enum):
    MENU = 1              # Главное меню
    PLAYING = 2           # Игровой процесс
    PAUSE = 3             # Пауза
    GAME_OVER = 4         # Поражение
    LEVEL_COMPLETE = 5    # Уровень пройден
```

## 🎨 Система отрисовки

### Порядок отрисовки:
1. Фон (градиент неба)
2. Задний план (облака, здания, кактусы)
3. Земля
4. Игровые объекты (спрайты)
5. HUD (интерфейс)
6. Оверлеи (пауза, меню)

### Фоны уровней:

#### Уровень 1 - Лес:
- Голубое небо с облаками
- Зеленые холмы
- Трава на земле

#### Уровень 2 - Город:
- Серое небо
- Силуэты зданий с окнами
- Асфальт с разметкой

#### Уровень 3 - Пустыня:
- Оранжевое небо
- Солнце
- Кактусы
- Песчаные барханы

## 🔧 Константы и настройки

```python
# Экран
SCREEN_WIDTH = 1200
SCREEN_HEIGHT = 700
FPS = 60

# Физика
PLAYER_SPEED = 5
JUMP_POWER = 15
GRAVITY = 0.8

# Размеры
PLAYER_WIDTH = 50
PLAYER_HEIGHT = 60
```

## 🎯 Логика уровней

### Уровень 1 (Лес):
```python
- Деревья: 5 шт (статичные)
- Мусор: 8 шт (пластик, не движется)
- Сложность: низкая
```

### Уровень 2 (Город):
```python
- Машины: 4 шт (движутся)
- Мусор: 12 шт (пластик, бумага, стекло)
- Движение: 50% мусора движется (ветер)
- Сложность: средняя
```

### Уровень 3 (Пустыня):
```python
- Токсичные бочки: 6 шт
- Мусор: 15 шт (все типы)
- 30% требует дрона
- Дрон активируется клавишей D
- Сложность: высокая
```

## 🔄 Игровой цикл

```python
while running:
    1. handle_events()    # Обработка ввода
    2. update()           # Обновление логики
    3. draw()             # Отрисовка
    4. clock.tick(FPS)    # Ограничение FPS
```

## 📊 Система подсчета очков

```python
# Очки за мусор
PLAYER_COLLECT = 10 очков
DRONE_COLLECT = 15 очков (бонус)

# Условие победы
if len(trash_group) == 0:
    state = LEVEL_COMPLETE

# Условие поражения
if health <= 0:
    state = GAME_OVER
```

## 🎨 Анимации

### Персонаж:
- Цикл ходьбы (руки и ноги)
- Поворот (flip по X)
- Индикатор мусора (мешок)

### Дрон:
- Вращение пропеллеров
- Парение (sine wave)
- Следование за игроком

### Объекты:
- Токсичные бочки (пульсация)
- Станция переработки (мерцание символа)
- Движущийся мусор (покачивание)

## 🛠️ Как добавить новый уровень

```python
def create_new_level(self):
    # 1. Очистка групп
    self.all_sprites.empty()
    self.trash_group.empty()
    self.obstacles_group.empty()

    # 2. Создание игрока
    self.player = Player(x, y)

    # 3. Создание станции
    self.recycling_station = RecyclingStation(x, y)

    # 4. Добавление препятствий
    for i in range(n):
        obstacle = Obstacle(...)
        self.obstacles_group.add(obstacle)

    # 5. Добавление мусора
    for i in range(m):
        trash = Trash(...)
        self.trash_group.add(trash)
```

## 🐛 Отладка

### Полезные точки отладки:
```python
# Проверка коллизий
print(f"Player rect: {player.rect}")
print(f"Trash count: {len(trash_group)}")

# Проверка состояния
print(f"Game state: {self.state}")
print(f"Health: {self.health}")
```

## 🚀 Оптимизация

### Производительность:
- Используйте группы спрайтов
- Ограничивайте частицы (не более 50-100)
- Очищайте неактивные объекты

### Память:
- Используйте Surface.convert_alpha()
- Переиспользуйте частицы
- Очищайте группы при смене уровня

## 📝 Будущие улучшения

Идеи для расширения:
1. ✅ Добавить звуковые эффекты
2. ✅ Сохранение рекордов
3. ✅ Больше типов мусора
4. ✅ Система достижений
5. ✅ Множитель очков
6. ✅ Бонусные предметы
7. ✅ Дополнительные уровни
8. ✅ Мультиплеер режим

## 📚 Полезные ресурсы

- [Pygame документация](https://www.pygame.org/docs/)
- [Python документация](https://docs.python.org/3/)
- [Sprite groups](https://www.pygame.org/docs/ref/sprite.html)

## 🤝 Контрибьюция

Для добавления нового функционала:
1. Изучите существующий код
2. Следуйте стилю кода
3. Тестируйте изменения
4. Документируйте код

---

**Удачи в разработке! 🚀**
